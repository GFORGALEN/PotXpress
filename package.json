{
  "name": "potxpress",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "npm@11.12.1",
  "engines": {
    "node": ">=24"
  },
  "workspaces": [
    "packages/*",
    "server",
    "client"
  ],
  "scripts": {
    "build": "npm run build --workspace @potxpress/contracts && npm run build --workspace @potxpress/client",
    "typecheck": "npm run build --workspace @potxpress/contracts && npm run typecheck --workspace @potxpress/server && npm run typecheck --workspace @potxpress/client",
    "test": "npm run build --workspace @potxpress/contracts && npm run test --workspace @potxpress/contracts && npm run test --workspace @potxpress/server && npm run test --workspace @potxpress/client",
    "dev:server": "npm run dev --workspace @potxpress/server",
    "dev:client": "npm run dev --workspace @potxpress/client",
    "db:migrate": "npm run db:migrate --workspace @potxpress/server"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^7.0.2"
  }
}

# PotXpress 测试报告（2026-08-26）

## 结论

本轮测试范围内未发现阻断发布或影响核心流程的功能性 Bug。

- 默认自动化测试：46 个，其中 45 个通过、1 个按环境开关跳过、0 个失败。
- 真实 PostgreSQL 集成测试：单独启用后 1/1 通过。
- TypeScript 类型检查：通过。
- 前端生产构建：通过。
- 浏览器控制台：所测页面无 error/warn。
- 375px 手机布局：无横向溢出。

## 已有单元与集成测试

项目已经有测试，不是空白状态。现有测试覆盖：

- 共享数据契约与 WebSocket 消息契约。
- 计时计算、暂停冻结、跨午夜与夏令时时区处理。
- CSV 转义与公式注入保护。
- 前端角色导航、计时显示、布局归一化、缩放、重叠判断。
- WebSocket 版本、门店隔离、重连退避。
- 登录与 tokenVersion 失效、API 权限、布局并发。
- 计时状态机、历史记录、重启恢复、WebSocket 鉴权。
- 文件存储并发、事务恢复、备份恢复与实例锁。

测试文件分布：

- `packages/contracts/test`：2 个测试。
- `server/test`：20 个测试，其中 1 个真实 PostgreSQL 测试本轮跳过。
- `client/test`：24 个测试。

## 执行结果

### `npm test`

- 通过：45
- 失败：0
- 跳过：1
- 默认跳过的是需要显式启用的真实 PostgreSQL 测试；本轮已在 Docker PostgreSQL 上单独执行并通过。

### `npm run typecheck`

- contracts：通过。
- server：通过。
- client：通过。

### `npm run build`

- 构建成功。
- 主 JavaScript 包约 614 kB（gzip 约 187 kB）。
- Vite 提示单个 chunk 超过 500 kB；这是性能优化项，不是功能错误。

## 浏览器冒烟测试

使用 Docker PostgreSQL 和 Dominion Road 单门店演示数据完成以下流程：

1. 演示员工快捷账号登录。
2. 桌台看板加载 26 张桌台并建立实时连接。
3. A1 开始计时并验证运行状态、倒计时和清台流程。
4. 今日记录页面加载并显示正确的空状态。
5. 退出登录并用系统管理员账号重新登录。
6. 巡检今日记录、桌台管理、门店设置、操作日志、门店管理、用户管理页面。
7. 在 375×812 视口检查移动导航和页面宽度，无横向溢出。
8. 所测流程没有浏览器控制台 error/warn。

测试后已清除临时计时和测试记录，26 张桌台均为空闲状态。

## 问题与风险分级

### P0/P1/P2 功能问题

未发现。

### P3 性能与测试完备性

1. 前端主包超过 Vite 默认的 500 kB 提示线。后续可按路由拆分管理员页面，降低首次加载体积。
2. 客户端现有测试主要覆盖纯函数和实时协议，尚未建立 React 组件级自动化测试环境。
3. 真实 PostgreSQL 集成测试需要通过独立环境开关执行，默认测试命令仍会跳过该项。
4. 本轮未包含多浏览器兼容性、长时间稳定性、压力、完整无障碍与视觉回归测试。

## 发布建议

当前代码可以继续进入联调或内部试用。正式上线前，建议把登录、桌台计时和管理员关键页面固化为可重复执行的端到端测试。
